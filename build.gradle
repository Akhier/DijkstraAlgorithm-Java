plugins {
    id 'java'
}

defaultTasks 'clean', 'build'

version = '3.0.3'
group = 'com.dragonheart.dijkstra'

repositories {
    mavenCentral()
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.12'
}
